# 📝 如何添加考試記錄

## 🚀 快速開始

有 **三種方法** 可以添加學生的考試記錄：

---

## 方法 1：使用網頁界面（推薦）⭐

### 步驟：

1. **確保開發服務器正在運行**
   ```bash
   cd wilburs-rewardbook
   npm run dev
   ```

2. **訪問首頁**
   - 打開 `http://localhost:3000`
   - 點擊學生卡片進入學生詳情頁

3. **點擊「➕ 添加評量」按鈕**
   - 在學生頁面右上角

4. **填寫表單**
   - **科目**：從下拉選單選擇（例如：國語、數學）
   - **評量名稱**：例如「第一次月考」
   - **評量類型**：選擇考試、小考、作業或專題
   - **得分**：如果已完成，填入分數；否則留空
   - **滿分**：默認 100 分
   - **日期**：截止或考試日期

5. **點擊「✅ 添加評量」**
   - 系統會自動計算獎金
   - 自動返回學生頁面

### 自動獎金規則：
- 💎 **滿分 (100分)** → **$30 鑽石**
- 🥇 **90-99分** → **$10 金錠**
- ⚙️ **80-89分** → **$5 鐵錠**
- 📉 **80分以下** → 無獎金

---

## 方法 2：使用 Supabase Dashboard

### 步驟 1：添加科目（如果還沒有）

1. 前往 **Supabase Dashboard** → **Table Editor**
2. 選擇 **subjects** 表
3. 點擊 **Insert** → **Insert row**
4. 填寫：
   ```
   student_id: (選擇學生 ID)
   name: 國語
   color: #4a9eff
   icon: 📖
   order_index: 0
   ```
5. 點擊 **Save**

### 步驟 2：添加評量

1. 在 **Table Editor** 選擇 **assessments** 表
2. 點擊 **Insert** → **Insert row**
3. 填寫：
   ```
   subject_id: (選擇科目 ID)
   title: 第一次月考
   assessment_type: exam
   score: 95
   max_score: 100
   percentage: 95
   status: completed
   reward_amount: 10
   due_date: 2024-11-01
   completed_date: 2024-11-01
   ```
4. 點擊 **Save**

### 步驟 3：添加交易記錄（獎金）

1. 在 **Table Editor** 選擇 **transactions** 表
2. 點擊 **Insert** → **Insert row**
3. 填寫：
   ```
   student_id: (學生 ID)
   assessment_id: (剛才創建的評量 ID)
   transaction_type: earn
   amount: 10
   description: 第一次月考獎金
   category: 測驗獎金
   ```
4. 點擊 **Save**

---

## 方法 3：使用 SQL（批量添加）

在 **Supabase SQL Editor** 執行：

```sql
-- 1. 查看學生 ID
SELECT id, name FROM students;

-- 2. 添加科目（替換成實際的學生 ID）
INSERT INTO subjects (student_id, name, color, icon, order_index)
VALUES 
  ('你的學生ID', '國語', '#4a9eff', '📖', 0),
  ('你的學生ID', '數學', '#ff4a6a', '🔢', 1),
  ('你的學生ID', '英文', '#4accff', '🌍', 2);

-- 3. 查看科目 ID
SELECT id, name, student_id FROM subjects;

-- 4. 批量添加評量（替換成實際的科目 ID）
INSERT INTO assessments (
  subject_id, 
  title, 
  assessment_type, 
  score, 
  max_score,
  percentage,
  status,
  reward_amount,
  due_date,
  completed_date
)
VALUES 
  ('科目ID', '第一次月考', 'exam', 95, 100, 95, 'completed', 10, '2024-11-01', '2024-11-01'),
  ('科目ID', '隨堂測驗', 'quiz', 88, 100, 88, 'completed', 5, '2024-11-08', '2024-11-08'),
  ('科目ID', '期中考', 'exam', 100, 100, 100, 'completed', 30, '2024-11-15', '2024-11-15');

-- 5. 批量添加交易記錄
INSERT INTO transactions (student_id, transaction_type, amount, description, category)
VALUES 
  ('你的學生ID', 'earn', 10, '第一次月考獎金', '測驗獎金'),
  ('你的學生ID', 'earn', 5, '隨堂測驗獎金', '測驗獎金'),
  ('你的學生ID', 'earn', 30, '期中考滿分獎金', '測驗獎金');
```

---

## 💡 提示

### 如果添加評量按鈕顯示「尚未添加科目」

您需要先添加科目。使用上述**方法 2**或**方法 3**先添加科目。

### 查看所有數據

```sql
-- 查看某學生的所有評量
SELECT 
  a.*,
  s.name as subject_name
FROM assessments a
JOIN subjects s ON a.subject_id = s.id
WHERE s.student_id = '你的學生ID'
ORDER BY a.due_date DESC;

-- 查看學生總餘額
SELECT * FROM student_summary WHERE student_id = '你的學生ID';
```

---

## 🎯 下一步

1. **添加更多科目**
2. **記錄每次考試**
3. **查看獎金累積**
4. **設置獎勵規則**（在 `reward_rules` 表）

需要幫助？隨時詢問！💪

